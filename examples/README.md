# Examples / fixtures

Public toy-site flows used for Phase-0 prototyping (the scaffolding `api-login` strategy,
the calibration→execution reproducibility keystone, the annotation renderer, the
ambiguity-contract under-specification stress test). Not first-consumer material.
