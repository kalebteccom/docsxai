# Examples / fixtures

Public toy-site flows used for Phase-0 prototyping (the `manual-capture` strategy against a
login-gated or no-auth toy site, the calibration→execution reproducibility keystone, the
annotation renderer, the ambiguity-contract under-specification stress test). Not first-consumer material.
