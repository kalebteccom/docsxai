# Claude Code subagents (mirror)

Expert agent definitions for docsxai development. Mirrored from `.agents/skills/`
(the cross-harness source of truth).

**Don't edit here** — edit `.agents/skills/<name>.md` and re-mirror. The mirror
exists so Claude Code's native subagent registry discovers these definitions with
zero config.
