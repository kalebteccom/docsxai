# Docs

Design notes, ADRs, and developer docs for the implementation.

The **canonical spec and roadmap** live in the `project-ideas` portfolio repo, under
`projects/automated-site-documentation-bot/` — that is the source of truth for *what*
we're building and *why*. This `docs/` tree is for *how* (implementation-level decisions).
