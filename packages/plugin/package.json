{
  "name": "@kalebtec/site-docs-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin — the first-class invocation surface. Calibration skills + deterministic commands + internal MCP registration.",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
