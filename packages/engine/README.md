# @kalebtec/site-docs-engine

LLM-agnostic engine: calibration pipeline stages (discovery, mapping+testing, commit) + flow-file parser/runtime + the target-site auth-strategy layer.

Scaffold only — implementation tracked in `PHASE-0.md` (repo root). Design lives in the
`project-ideas` portfolio under `projects/automated-site-documentation-bot/`.
