// Library surface of the bare `docsxai` package — a re-export of @docsxai/engine.
// `import { parseFlowFile } from "docsxai"` and `from "@docsxai/engine"` are equivalent;
// the engine is the library. Depend on @docsxai/engine directly for a minimal tree.

export * from "@docsxai/engine";
