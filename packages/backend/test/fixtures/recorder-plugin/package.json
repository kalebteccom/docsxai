{
  "name": "site-docs-test-recorder-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "Local path publisher plugin fixture — records publish() calls into the workspace.",
  "docsxai": {
    "namespace": "recorder",
    "kind": "publisher",
    "register": "register.mjs"
  }
}
